let obj = {
    name: 'JavaScript',
    estd: '1995',
    ranking: 1,
    founder: 'Brendan Eich'
}

console.log(obj)
let keysRst = Object.keys(obj)
let valuesRst = Object.values(obj)
let entriesRst = Object.entries(obj)
console.log(keysRst)
console.log(valuesRst)
console.log(entriesRst)